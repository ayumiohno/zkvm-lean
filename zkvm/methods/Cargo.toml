[package]
name = "methods"
version = "0.1.0"
edition = "2021"

[dependencies]
methods-env = { path = "../methods-env" }

[build-dependencies]
risc0-build = { version = "^3.0.6" }
methods-env = { path = "../methods-env" }

[package.metadata.risc0]
methods = ["guest", "thmcheck", "parseprobe"]
